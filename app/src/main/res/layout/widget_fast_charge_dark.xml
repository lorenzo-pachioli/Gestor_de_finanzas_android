<?xml version="1.0" encoding="utf-8"?>
<!-- app/src/main/res/layout/widget_fast_charge_dark.xml -->
<!--
    DARK MODE — Full widget.
    Root is fully transparent so the wallpaper shows through.
    The inner button row gets the dark neomorphism drawable.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@android:color/transparent"
    android:weightSum="10">

    <!-- Top spacer -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:background="@android:color/transparent" />

    <!--
        Button row: dark frosted-glass card.
        Uses bg_widget_button_dark.xml for the glowing teal border + shadow.
    -->
    <LinearLayout
        android:id="@+id/widget_root"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="6"
        android:background="@drawable/bg_widget_button_dark"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="6dp">

        <!-- Logo tap area (navigates to MainActivity) -->
        <ImageView
            android:id="@+id/widget_logo"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:src="@drawable/fluxus_logo"
            android:padding="6dp"
            android:adjustViewBounds="true" />

        <!-- Divider -->
        <FrameLayout
            android:layout_width="1dp"
            android:layout_height="match_parent"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="12dp"
            android:background="#4D00A896"
            android:layout_marginHorizontal="4dp" />

        <!-- "Crear" label with dark pill -->
        <FrameLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:padding="6dp">

            <TextView
                android:id="@+id/widget_text"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:text="@string/crear"
                android:textColor="#DBEDFE"
                android:textSize="16sp"
                android:textStyle="bold"
                android:gravity="center"
                android:background="@drawable/bg_widget_create_pill_dark" />

        </FrameLayout>

    </LinearLayout>

    <!-- Bottom spacer -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:background="@android:color/transparent" />

</LinearLayout>
